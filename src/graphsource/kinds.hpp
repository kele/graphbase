#pragma once

namespace graphbase {
namespace graphsource {

struct AllUndirected {
  size_t min_size;
  size_t max_size;
};

enum class Kind { All };

}  // namespace graphsource
}  // namespace graphbase
